/**
 * PRODUCTION environment.
 * Used by `ng build` (default configuration = production).
 */
export const environment = {
  production: true,
  name: 'production',
  // Placeholder for the future backend / database API.
  // When you add follower features, point this at your prod API.
  apiUrl: 'https://api.jacobychye.com',
};
