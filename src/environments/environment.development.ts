/**
 * DEVELOPMENT environment.
 * Swapped in automatically by `ng serve` / `ng build --configuration development`.
 */
export const environment = {
  production: false,
  name: 'development',
  // Local backend / database API for when you start building follower features.
  apiUrl: 'http://localhost:3000',
};
